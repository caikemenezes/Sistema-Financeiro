# Copie este arquivo pra ".env" e preencha os valores reais.
# NUNCA commite o ".env" de verdade (já está no .gitignore).

# Connection string do banco MySQL (formato: mysql://usuario:senha@host:3306/banco)
DATABASE_URL="mysql://usuario:senha@host:3306/nome_do_banco"
